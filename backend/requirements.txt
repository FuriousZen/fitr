# fitr backend, pinned to versions actually installed and tested on
# Python 3.12 / Debian 12 (aarch64) / PostgreSQL 15 + pgvector 0.8.6.
#
# torch/torchvision must come from the CPU wheel index, which cannot be
# expressed portably in a plain requirements.txt. Install them first:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# then `pip install -r requirements.txt`. See backend/README.md.

Flask==3.1.3
flask-cors==6.0.5
Flask-SQLAlchemy==3.1.1
SQLAlchemy>=2.0.51,<2.1
psycopg[binary]==3.3.4
pgvector==0.5.0

# CLIP. transformers 5.x changed get_image_features() to return
# BaseModelOutputWithPooling instead of a bare tensor; app/services/clip.py
# handles both shapes, but the pin documents what was tested.
transformers>=5.14,<6
pillow==12.3.0
numpy==2.5.1

google-genai==2.17.0
requests==2.34.2
python-dotenv==1.2.2
gunicorn==26.0.0

# dev / test
pytest==9.1.1
pytest-cov
responses==0.26.2
